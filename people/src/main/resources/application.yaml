spring:
  application:
    name: people

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs/v1
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
    enabled: true
